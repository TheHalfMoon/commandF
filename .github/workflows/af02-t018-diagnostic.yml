name: af02-t018-diagnostic

on:
  push:
    branches:
      - feat/af02-a0-corpus-assertion-contract

permissions:
  contents: read

jobs:
  verifier-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5 / Node 24
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@032958afbdc797a9164d3bc0b56325c1308924a5 # 1.97.1
      - name: Capture AF-02 verifier test output
        shell: bash
        run: |
          set +e
          cargo test --locked --manifest-path tools/af02-verifier/Cargo.toml -- --nocapture > af02-t018-verifier-tests.log 2>&1
          status=$?
          printf '%s\n' "$status" > af02-t018-verifier-tests.status
          exit 0
      - name: Upload AF-02 T018 diagnostic output
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: af02-t018-verifier-tests
          path: |
            af02-t018-verifier-tests.log
            af02-t018-verifier-tests.status
          if-no-files-found: error
          retention-days: 1
