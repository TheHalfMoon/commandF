# CF-03 Tasks

Status: Implementation and convergence complete — founder review candidate

- [x] **T001 — Shared bounded resource scanner.** CF-02 package-root JSON scanning is factored into one internal helper used by inspect and diff with unchanged filtering and archive/resource bounds.
- [x] **T002 — Diff models.** `StructuralDiffReport`, package evidence, resource keys, structural change kinds, and change records are deterministic and contain no severity/compatibility fields.
- [x] **T003 — Two-state package loading.** `commandf diff` selects exactly one package version from each explicit lock, verifies each CF-01 cache object, reads the content-addressed archives, and independently rechecks archive digests during structural parsing.
- [x] **T004 — Deterministic resource matcher.** Unique canonical URLs match logically; multi-version URL groups use exact `url|version` and require usable versions for every member; non-canonical resources fall back through unique `resourceType/id` then filename; ambiguity and duplicate archive filenames fail closed.
- [x] **T005 — StructureDefinition shape parser.** Selected structural metadata plus snapshot/differential element objects are compared using exact `ElementDefinition.id` addresses.
- [x] **T006 — Structural validation and normalization.** CF-03-owned field shapes are validated before normalization; valid FHIR primitive `_field` metadata forms are preserved; known set-like fields are normalized; order-sensitive fields including `extension[]` remain ordered.
- [x] **T007 — Typed diff engine.** Resource/view/element additions/removals and selected resource/StructureDefinition field changes are emitted without CF-04 compatibility classification.
- [x] **T008 — Deterministic serialization.** Changes are stable-sorted and identical inputs serialize byte-identically; self-diff is empty.
- [x] **T009 — CLI.** `commandf diff <package-name>` uses explicit before/after lock/cache paths and `--format json`; it performs no acquisition.
- [x] **T010 — Synthetic and CLI contract tests.** Coverage includes no-op/determinism, unique and multi-version canonical matching, missing multiplicity versions, ambiguity failure, duplicate filename failure, view/element additions and removals, structural field changes, malformed interpreted-field rejection, primitive metadata compatibility, editorial exclusion, set-like normalization, extension-order preservation, CLI usage/error behavior, corrupted caches, and offline success.
- [x] **T011 — Real self-diff smoke.** CI independently resolves/verifies published `hl7.fhir.r4.core@4.0.1` into separate before/after cache+lock states, inspects the before state, runs the real CLI, and proves `changes` is empty.
- [x] **T012 — Review/convergence.** Exact implementation evidence head `a8fec0f2073abe1b9660457a493db6c2e437a2ec` passed run `31761209998`: Format PASS, Clippy `--locked` PASS, Test `--locked` PASS, and official R4 independent-resolution inspect+self-diff smoke PASS. Manual CodeRabbit review returned three actionable findings: malformed interpreted-field validation was fixed with regression coverage; the proposal to copy the first CI state was not adopted because independent resolution is an explicit acceptance requirement and CodeRabbit withdrew that finding; global `extension[]` sorting was not adopted by design and CodeRabbit withdrew that finding because CF-03 preserves order absent profile-aware unordered semantics. All three threads are resolved. `/review` was requested from Qodo and no Qodo result has been observed, so no Qodo PASS is claimed. PR remains Draft and CF-04 severity logic is not introduced.
